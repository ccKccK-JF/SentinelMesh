global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: sentinelmesh-control-plane
    metrics_path: /metrics
    static_configs:
      - targets:
          - control-plane:8080
